#include <stdio.h>
int main() {
	float length, width;
	printf("Enter The  length : ");
	scanf("%f",&length);
	printf("Enter The width : ");
	scanf("%f",&width);
	printf("The Area : %f ",(length * width));
	return 0;
}

