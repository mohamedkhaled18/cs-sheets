#include <stdio.h>
int main() {
	int length = 25, width = 60, height;
	if (length = 50)
		height = 4;
	else
		height = 8;
	printf("Length = %d, Width = %d, and Height = %d.", length, width, height);
	return 0;
}

